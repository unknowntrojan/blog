---
layout: default
---

<div class="bg-zinc-800 m-auto p-4 mb-8 flex flex-row" markdown="0">
  <p class="m-auto ml-16 whitespace-nowrap">
    <i class="fa-regular fa-calendar mr-1"></i>
    {{ data.date | date: "%-d %B %Y" }}
  </p>
  <h1 class="text-center mb-0 m-auto">{{ data.title }}</h1>
  <p class="m-auto mr-16 whitespace-nowrap">
    <i class="fa-regular fa-clock mr-1"></i>
    {{ data.content | reading_time }}
  </p>
</div>

<div class="mx-4">
  {{ content }}
</div>